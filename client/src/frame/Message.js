import {defineMessages} from "react-intl";

export default defineMessages({
    "title": {
        "id": "app.title",
        "description": "Application title",
        "defaultMessage": "Code Maater"
    }
});
